# Portfolio SMTP Configuration - TODO

## Completed Steps

- [x] 1. Create `.env.local` with SMTP credentials
- [x] 2. Update `.gitignore` to protect environment files
- [x] 3. Create `src/lib/smtp.ts` - shared SMTP utility with connection pooling
- [x] 4. Refactor `/api/contact/route.ts` - use env vars + connection pooling
- [x] 5. Refactor `/api/resume/route.ts` - use env vars + connection pooling

## Verification

- [x] 6. Build and test the application - BUILD SUCCESS
